# Административная панель для сервиса BAS (Browser Agent Service)

### Установка зависимостей

- Выполнить команду `npm install`

### Запуск в dev режиме

- Выполнить команду `npm start`

### Сборка релизной сборки

- Выполнить команду `npm run build`
